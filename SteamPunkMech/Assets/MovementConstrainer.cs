using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementConstrainer : MonoBehaviour
{/*

    private float startX;
    private float startY;
    private float startZ;

    private float startRotationX;
    private float startRotationY;
    private float startRotationZ;

    public bool constrainX;
    public bool constrainY;
    public bool constrainZ;

    public bool constrainRotationX;
    public bool constrainRotationY;
    public bool constrainRotationZ;

    // Use this for initialization
    void Start () {

        startX = transform.position.x;
        startY = transform.position.y;
        startZ = transform.position.z;

        startRotationX = transform.rotation.x;
        startRotationY = transform.rotation.y;
        startRotationZ = transform.rotation.z;
}
	
	// Update is called once per frame
	void Update () {
	    if (constrainX)
	    {
	        gameObject.transform.position.x = startX;
	    }
	    if (constrainY)
	    {
	        gameObject.transform.position.y = startY;
	    }
	    if (constrainZ)
	    {
	        gameObject.transform.position.z = startZ;
	    }

	    if (constrainRotationX)
	    {
	        gameObject.transform.rotation.x = startX;
	    }
	    if (constrainRotationY)
	    {
	        gameObject.transform.rotation.y = startY;
	    }
	    if (constrainRotationZ)
	    {
	        gameObject.transform.rotation.z = startZ;
	    }

    }*/
}
